<%= link_to 'Test Guru', root_path %>
<% if logged_in? %>
  <p class="nav user">Welcom, <b><%= current_user.name %> - <%= current_user.email %></b> Guru | <%= link_to 'logout', session_path(session), method: :delete %></p>
<% else %>
  <p class="nav user"><%= link_to 'login', login_path %> | <%= link_to 'create account', signup_path %></p>
<% end %>
